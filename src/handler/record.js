//  This domain define Record class

class Record {
  constructor (key, createdAt, totalCount) {
    this.key = key
    this.createdAt = createdAt
    this.totalCount = totalCount
  }
};

module.exports = Record
